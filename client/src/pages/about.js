import React from "react"
import { Link } from "react-router-dom"

export default () => {
    return (
        <div>
            <p>About (reacte)</p>
            <Link to="/">Home</Link>
        </div>
    )
}